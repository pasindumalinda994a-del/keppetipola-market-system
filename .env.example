# Public API base URL used by the Next.js frontend (no trailing slash).
# Local: leave unset or use http://localhost:5000
# Production (Vercel): set to your deployed backend URL, then redeploy
NEXT_PUBLIC_API_URL=http://localhost:5000
