MONGODB_URI=mongodb://127.0.0.1:27017/keppetipola-market
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
# Optional override; leave empty for same-origin /api
NEXT_PUBLIC_API_URL=
# Optional SMTP for password-reset codes. If unset, codes are logged in the server console.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Keppetipola Market <noreply@example.com>"
